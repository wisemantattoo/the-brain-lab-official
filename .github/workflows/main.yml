name: Daily Video Bot

on:
  schedule:
    - cron: '0 8 * * *' # ירוץ כל יום ב-8 בבוקר
  workflow_dispatch:      # מאפשר הרצה ידנית

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y imagemagick
        # תיקון קריטי ל-MoviePy כדי שיעבוד עם טקסט
        sudo sed -i 's/none/read,write/g' /etc/ImageMagick-6/policy.xml

    - name: Install Python Dependencies
      run: |
        pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client moviepy==1.0.3 imageio==2.4.1

    - name: Run Bot
      env:
        # הנה החיבורים שהיו חסרים!
        CLIENT_SECRET_JSON: ${{ secrets.CLIENT_SECRET_JSON }}
        YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
      run: python main.py
